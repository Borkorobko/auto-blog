name: Related Guides Test V2

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  related-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --upgrade pip openai

      - name: Preview improved related guides
        run: python scripts/rewrite_placeholders.py --file agility-drills-football.html --related-only --dry-run

      - name: Refresh improved related guides
        run: python scripts/rewrite_placeholders.py --file agility-drills-football.html --related-only

      - name: Show changed files
        run: git status --short

      - name: Commit improved related guides
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add posts/agility-drills-football.html
          git commit -m "Improve related guide relevance" || echo "No changes to commit"
          git push origin HEAD:main

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
